# Быстрый старт OmniChat

## Пошаговая инструкция запуска

### Шаг 1: Проверка зависимостей

Убедитесь, что установлены:
- ✅ Node.js 18+ (проверьте: `node -v`)
- ✅ PostgreSQL 12+ (должен быть запущен)

### Шаг 2: Настройка базы данных PostgreSQL

1. Откройте PostgreSQL (pgAdmin или psql)

2. Создайте базу данных:
```sql
CREATE DATABASE omnichat;
```

3. Запомните данные для подключения:
   - Host: обычно `localhost`
   - Port: обычно `5432`
   - Database: `omnichat`
   - User: обычно `postgres`
   - Password: ваш пароль PostgreSQL

### Шаг 3: Настройка Backend

1. Перейдите в папку backend:
```powershell
cd backend
```

2. Создайте файл `.env` (скопируйте из `.env.example` если есть, или создайте новый):
```powershell
# Если есть .env.example
Copy-Item .env.example .env

# Или создайте новый файл .env
```

3. Откройте `backend/.env` и настройте:
```env
# Server
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database - ВАЖНО: укажите ваши данные!
DB_HOST=localhost
DB_PORT=5432
DB_NAME=omnichat
DB_USER=postgres
DB_PASSWORD=ваш_пароль_postgresql

# JWT - измените на свой секретный ключ
JWT_SECRET=my-super-secret-key-change-in-production-12345
JWT_EXPIRES_IN=24h

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
```

4. Инициализируйте базу данных (создаст все таблицы):
```powershell
npm run migrate
```

5. Создайте первого администратора:
```powershell
npm run create-admin admin admin123 "Администратор"
```

**Важно:** После первого запуска измените пароль администратора!

### Шаг 4: Настройка Frontend

1. Перейдите в папку frontend:
```powershell
cd ..\frontend
```

2. Создайте файл `.env`:
```powershell
# Создайте файл .env
```

3. Добавьте в `frontend/.env`:
```env
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_SOCKET_URL=http://localhost:5000
```

### Шаг 5: Запуск приложения

#### Вариант 1: Запуск обоих серверов одновременно (рекомендуется)

Из корневой папки проекта:
```powershell
cd C:\Users\User\Desktop\OmniChat
npm run dev
```

Это запустит:
- Backend на http://localhost:5000
- Frontend на http://localhost:3000

#### Вариант 2: Запуск отдельно

**Терминал 1 - Backend:**
```powershell
cd C:\Users\User\Desktop\OmniChat\backend
npm run dev
```

**Терминал 2 - Frontend:**
```powershell
cd C:\Users\User\Desktop\OmniChat\frontend
npm start
```

### Шаг 6: Вход в систему

1. Откройте браузер: http://localhost:3000
2. Войдите с учетными данными:
   - **Логин:** `admin`
   - **Пароль:** `admin123`

### Проверка работы

После запуска проверьте:

1. **Backend работает:**
   - Откройте: http://localhost:5000/api/health
   - Должен вернуться: `{"status":"ok","timestamp":"..."}`

2. **Frontend работает:**
   - Откройте: http://localhost:3000
   - Должна открыться страница входа

3. **База данных подключена:**
   - В логах backend не должно быть ошибок подключения к БД
   - Если есть ошибки - проверьте настройки в `backend/.env`

## Решение проблем

### Ошибка подключения к базе данных

```
Error: connect ECONNREFUSED 127.0.0.1:5432
```

**Решение:**
1. Убедитесь, что PostgreSQL запущен
2. Проверьте настройки в `backend/.env`:
   - `DB_HOST=localhost`
   - `DB_PORT=5432` (или ваш порт)
   - `DB_USER` и `DB_PASSWORD` правильные

### Ошибка "Cannot find module"

```powershell
# Переустановите зависимости
cd backend
npm install

cd ../frontend
npm install --legacy-peer-deps
```

### Порт уже занят

Если порт 5000 или 3000 занят:

1. Измените порт backend в `backend/.env`:
```env
PORT=5001
```

2. Обновите `frontend/.env`:
```env
REACT_APP_API_URL=http://localhost:5001/api
REACT_APP_SOCKET_URL=http://localhost:5001
```

### База данных не создается

Убедитесь, что:
1. PostgreSQL запущен
2. У вас есть права на создание баз данных
3. Пользователь и пароль правильные

## Следующие шаги

После успешного запуска:

1. ✅ Смените пароль администратора
2. ✅ Создайте операторов через админ-панель
3. ✅ Настройте каналы связи
4. ✅ Добавьте быстрые фразы (шаблоны)

## Полезные команды

```powershell
# Остановка серверов
# Нажмите Ctrl+C в терминале

# Просмотр логов backend
# Логи сохраняются в backend/logs/

# Пересоздание базы данных
# Удалите базу и запустите: npm run migrate

# Создание нового оператора
cd backend
npm run create-admin логин пароль "ФИО"
```

## Структура проекта

```
OmniChat/
├── backend/          # Backend API (Node.js + Express)
│   ├── src/         # Исходный код
│   ├── .env         # Настройки (создайте!)
│   └── uploads/     # Загруженные файлы
├── frontend/         # Frontend (React)
│   ├── src/         # Исходный код
│   └── .env         # Настройки (создайте!)
└── package.json      # Корневой package.json
```

---

**Готово!** Теперь ваша система омниканального call-центра должна работать. 🚀
