# Настройка Telegram для локальной разработки (БЕЗ ngrok)

## Простой способ - Polling (рекомендуется для локальной разработки)

Система автоматически использует **polling** режим для локальной разработки. Вам не нужен ngrok!

### Шаг 1: Добавьте токен в .env

В файле `backend/.env` добавьте:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

### Шаг 2: Перезапустите Backend

```powershell
cd backend
npm run dev
```

В логах вы увидите:
```
Telegram polling started (for local development)
```

### Шаг 3: Готово!

Теперь:
1. Отправьте сообщение вашему боту в Telegram
2. Сообщение появится в интерфейсе оператора
3. Ответьте в интерфейсе - сообщение отправится в Telegram

**Всё! Никакого ngrok не нужно!**

---

## Альтернатива: Настройка ngrok (если нужен webhook)

Если вы всё же хотите использовать webhook (например, для production), настройте ngrok:

### Шаг 1: Регистрация в ngrok

1. Зайдите на https://dashboard.ngrok.com/signup
2. Зарегистрируйтесь (можно через GitHub/Google)
3. После регистрации перейдите в https://dashboard.ngrok.com/get-started/your-authtoken
4. Скопируйте ваш authtoken

### Шаг 2: Установка authtoken

```powershell
ngrok config add-authtoken ваш_authtoken
```

### Шаг 3: Запуск ngrok

```powershell
ngrok http 5000
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

### Шаг 4: Настройка webhook

Откройте в браузере:
```
https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook?url=https://abc123.ngrok.io/api/telegram/webhook
```

Или используйте curl:
```powershell
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook?url=https://abc123.ngrok.io/api/telegram/webhook"
```

### Шаг 5: Проверка webhook

```powershell
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

---

## Какой способ выбрать?

### Polling (рекомендуется для локальной разработки)
✅ Не требует регистрации  
✅ Не требует настройки  
✅ Работает сразу  
❌ Менее эффективен для production

### Webhook (рекомендуется для production)
✅ Более эффективен  
✅ Мгновенные обновления  
✅ Лучше для production  
❌ Требует публичный URL (ngrok или сервер)

---

## Важно

- **Для локальной разработки** используйте **polling** - он уже настроен автоматически!
- **Для production** используйте **webhook** с вашим сервером
- Polling автоматически отключается в production режиме (`NODE_ENV=production`)

---

## Проверка работы

1. Убедитесь, что токен в `.env`: `TELEGRAM_BOT_TOKEN=...`
2. Перезапустите backend
3. В логах должно быть: `Telegram polling started`
4. Отправьте сообщение боту в Telegram
5. Проверьте интерфейс оператора - должен появиться новый диалог

Готово! 🚀
